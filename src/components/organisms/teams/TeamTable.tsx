import {memo, VFC} from "react";

type Props = {}
export const TeamTable:VFC<Props> = memo((props) => {
    //現在ページ
    //テーブルの行数
    //編集ボタンのクリック
    //削除ボタンのクリック

    return (
        // テーブルヘッダー
        // テーブル
        // ページネーション
        // モーダル
        <div>
        </div>
    )
})